/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'wineworld.lk', 'ws23.lk'],
  },
};

module.exports = nextConfig;
